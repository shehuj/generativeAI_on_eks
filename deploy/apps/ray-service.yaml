---
apiVersion: v1
kind: Namespace
metadata:
  name: dogbooth
---
apiVersion: ray.io/v1
kind: RayService
metadata:
  name: dogbooth
  namespace: dogbooth
spec:
  # Serve config in the v2 (multi-application) format required by KubeRay >= 1.0.
  serveConfigV2: |
    applications:
      - name: dogbooth
        import_path: dogbooth:entrypoint
        route_prefix: /
        runtime_env:
          env_vars:
            MODEL_ID: "askulkarni2/dogbooth"
            # MODEL_ID: "stabilityai/stable-diffusion-2-1"
  rayClusterConfig:
    rayVersion: '2.9.0'
    headGroupSpec:
      rayStartParams:
        dashboard-host: '0.0.0.0'
      template:
        spec:
          containers:
            - name: ray-head
              image: docker.io/captcloud01/dogbooth:ray2.9.1
              resources:
                limits:
                  cpu: 2
                  memory: 16Gi
                  nvidia.com/gpu: 1
                requests:
                  cpu: 2
                  memory: 16Gi
                  nvidia.com/gpu: 1
              ports:
                - containerPort: 6379
                  name: gcs-server
                  protocol: TCP
                - containerPort: 8265
                  name: dashboard
                  protocol: TCP
                - containerPort: 10001
                  name: client
                  protocol: TCP
                - containerPort: 8000
                  name: serve
                  protocol: TCP
    workerGroupSpecs:
      # Worker scaled to 0 and CPU-only: the single-replica Stable Diffusion model
      # (1 GPU) runs on the head's GPU. This keeps each Ray cluster at 1 GPU so a
      # KubeRay zero-downtime upgrade (old + new clusters) fits the 2 GPU nodes.
      - replicas: 0
        minReplicas: 0
        maxReplicas: 5
        rayStartParams: {}
        groupName: small-group
        template:
          spec:
            containers:
              - name: ray-worker
                image: docker.io/captcloud01/dogbooth:ray2.9.1
                lifecycle:
                  preStop:
                    exec:
                      command: ["/bin/sh","-c","ray stop"]
                resources:
                  limits:
                    cpu: "2"
                    memory: "16Gi"
                  requests:
                    cpu: "2"
                    memory: "16Gi"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dogbooth
  namespace: dogbooth
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: "/$1"
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
        # Ray Dashboard
        - path: /dogbooth/(.*)
          pathType: ImplementationSpecific
          backend:
            service:
              name: dogbooth-head-svc
              port:
                number: 8265
        # Ray Serve
        - path: /dogbooth/serve/(.*)
          pathType: ImplementationSpecific
          backend:
            service:
              name: dogbooth-head-svc
              port:
                number: 8000
